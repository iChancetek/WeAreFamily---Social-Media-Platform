apiVersion: v1
kind: Service
metadata:
  name: {{ include "famio.fullname" . }}
  labels:
    {{- include "famio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "famio.selectorLabels" . | nindent 4 }}
